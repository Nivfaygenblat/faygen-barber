"use client";import { useState } from "react";import { createClient } from "@/lib/supabase/client";export default function ResetPassword(){const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(password.length<8){setMessage("הסיסמה חייבת להכיל לפחות 8 תווים.");return}const db=createClient();const {error}=db?await db.auth.updateUser({password}):{error:new Error()};setMessage(error?"הקישור אינו תקין או שפג תוקפו.":"הסיסמה עודכנה. אפשר להתחבר עכשיו.")}return <div className="login-page"><form className="login-card" onSubmit={submit}><h1>בחירת סיסמה חדשה</h1><div className="field"><label>סיסמה חדשה</label><input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></div>{message&&<p>{message}</p>}<button className="button">שמירת הסיסמה</button><a className="text-button" href="/admin/login">חזרה להתחברות</a></form></div>}
